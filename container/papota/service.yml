apiVersion: v1
kind: Service
metadata:
  name: hello-world-service
spec:
  type: LoadBalancer # Expose the service externally
  selector:
    app: hello-world
  ports:
  - port: 80
    targetPort: 80
